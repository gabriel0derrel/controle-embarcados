# Permitir conexões sem autenticação (bom para desenvolvimento local)
allow_anonymous true

# Porta padrão para o ESP32 e Backend NestJS (Escutando em todas as interfaces)
listener 1883 0.0.0.0
protocol mqtt

listener 9001 0.0.0.0
protocol websockets
